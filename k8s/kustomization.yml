apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yml
  - service.yml

configMapGenerator:
  - name: delivery-config
    files:
      - application.yml
    options:
      labels:
        app: delivery-service